import React from "react";
import { Apple, Play } from "lucide-react";
import { FaApple } from "react-icons/fa";// Optional icons (can remove if not needed)

const HeroSection = () => {
    return (
        <div></div>
    );
};

export default HeroSection;
